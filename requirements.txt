# Core AWS dependencies (already in Lambda layer)
boto3>=1.26.0
botocore>=1.29.0

# Strands dependencies (should be in EFS mount)
# strands>=1.0.0  # Comment out - available in EFS

# Additional dependencies for marketing campaigns
requests>=2.28.0
pandas>=1.5.0  # For CSV processing
numpy>=1.24.0  # For data analysis

# Development/testing dependencies
pytest>=7.0.0
moto>=4.0.0  # For mocking AWS services